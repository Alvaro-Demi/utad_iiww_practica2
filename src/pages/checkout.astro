---
import MainLayout from "../layouts/Layout.astro";
---

<MainLayout>
  <section class="hero">
    <h1 class="hero__title">Checkout</h1>
    <p class="hero__subtitle">
      Introduce tus datos de facturación y el método de pago para completar la
      suscripción.
    </p>

    <div class="hero__actions">
      <a class="btn" href="/carrito">Volver al carrito</a>
      <a class="btn" href="/productos">Ver productos</a>
    </div>
  </section>

  <!-- Contenido principal: formularios + resumen -->
  <section class="section checkout reveal">
    <!-- Columna izquierda: Formulario -->
    <div class="card">
      <h2>Datos de facturación</h2>

      <form action="/checkout" method="post">
        <div class="form-grid">
          <div class="field">
            <label for="nombre">Nombre</label>
            <input id="nombre" name="nombre" type="text" required />
          </div>

          <div class="field">
            <label for="apellidos">Apellidos</label>
            <input id="apellidos" name="apellidos" type="text" required />
          </div>

          <div class="field">
            <label for="email">Correo electrónico</label>
            <input id="email" name="email" type="email" required />
          </div>

          <div class="field">
            <label for="telefono">Teléfono (opcional)</label>
            <input id="telefono" name="telefono" type="text" />
          </div>

          <div class="field" style="grid-column: 1 / -1;">
            <label for="direccion">Dirección</label>
            <input id="direccion" name="direccion" type="text" required />
          </div>

          <div class="field">
            <label for="ciudad">Ciudad</label>
            <input id="ciudad" name="ciudad" type="text" required />
          </div>

          <div class="field">
            <label for="cp">Código postal</label>
            <input id="cp" name="cp" type="text" required />
          </div>

          <div class="field" style="grid-column: 1 / -1;">
            <label for="pais">País</label>
            <input id="pais" name="pais" type="text" required />
          </div>
        </div>

        <div class="divider"></div>

        <h2>Datos de pago</h2>

        <div class="form-grid">
          <div class="field" style="grid-column: 1 / -1;">
            <label for="tarjeta">Número de tarjeta</label>
            <input
              id="tarjeta"
              name="tarjeta"
              type="text"
              placeholder="1234 5678 9012 3456"
              required
            />
          </div>

          <div class="field">
            <label for="caducidad">Caducidad</label>
            <input id="caducidad" name="caducidad" type="month" required />
          </div>

          <div class="field">
            <label for="cvv">CVV</label>
            <input id="cvv" name="cvv" type="text" placeholder="123" required />
          </div>
        </div>

        <div
          style="margin-top: 16px; display:flex; gap: 12px; flex-wrap: wrap;"
        >
          <button class="btn btn--primary" type="submit">Confirmar pago</button>
          <button class="btn" type="button">Guardar y continuar luego</button>
        </div>
      </form>
    </div>

    <!-- Columna derecha: Resumen -->
    <aside class="card">
      <h2>Resumen del pedido</h2>

      <p class="card__text">Plan seleccionado: <strong>Plan Pro</strong></p>
      <p class="card__text">Facturación: <strong>Mensual</strong></p>
      <p><strong>Total: 19,99 €/mes</strong></p>

      <div class="divider"></div>

      <p class="card__text">
        Al confirmar el pago, se activará tu suscripción y podrás acceder a
        todas las herramientas incluidas en tu plan.
      </p>
    </aside>
  </section>
</MainLayout>
